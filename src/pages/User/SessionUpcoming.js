import React from 'react'
import SessionCard from './SessionCard'

function SessionUpcoming() {
  const status = "Upcoming"
  return (
    <SessionCard status={status}/>
  )
}

export default SessionUpcoming